<h1><%= @chapter.title %></h1>

<p><%= @chapter.text %></p>

<% if @chapter.number == 1 %>
  <p>
    Once upon a time, deep in a
    <%= render "shared/landscape_type_form" , object: @book %>
    lived a
    <%= render "shared/family_type_form" , object: @book%>
    family.  There was a great big Papa <%= @book.family_type %>, a medium sized Mama <%= @book.family_type %>, and a wee little Baby <%= @book.family_type %>.
  </p>

<% elsif @chapter.number == 2 %>
  <p>
    Now, one morning, Mama <%= @book.family_type %> made
    <%= render "shared/food_form" , object: @book%>
    for breakfast. The <%= @book.food %> was very hot. Papa <%= @book.family_type %> said, "Why don’t we take a walk in the forest while the <%= @book.food %> is cooling down." So, they went for a walk.
  </p>
<% end %>

<h3><%= link_to "Next Chapter", book_chapter_path(@book, @chapter.number + 1) %></h3>
